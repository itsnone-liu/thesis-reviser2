# 论文系统修改记录

> 日期：2026-06-28
> 工作目录：`/root/project/workspace/thesis-reviser/`
> 源文件目录：`/root/project/workspace/thesis-reviser/`（旧版文件保留为 `ref_*.py`）

---

## 一、代码总体架构

```
thesis_reviser/
├── core.py          # 基础层：LLM调用、DOCX排版、图表/图片渲染、标签解析、最终排版
├── generator.py     # 生成管道：调用LLM输出纯文本（含<chart/><table/><drawing/>标签）
├── renderer.py      # 渲染管道：无LLM，解析标签→生成图表/图片→组装DOCX
├── profile.py       # 独立画像生成模块（可嵌入/独立CLI）
├── reviser.py       # 修改管道：分析→诊断→重写（调用generator+renderer）
├── web.py           # FastAPI Web服务
├── prompts/         # 提示词模板
│   ├── manage.py    # 经管类（含图表约束、格式铁律、致命错误检查）
│   └── design.py    # 设计类（含图纸标签约束、图纸补齐）
├── ref_*.py         # 旧版参考文件（不动）
└── output/          # 测试输出
```

---

## 二、本次修改汇总

### 0️⃣ `renderer.py` — 封面自动补齐与输入侧车文件加载

**问题**：渲染时即便已有学生信息，封面仍可能不完整；未显式传 `--cover` 时，封面信息不进入渲染链路。

**修复**：
- `renderer.py` 支持显式 `--cover` 传入封面 JSON
- 若未传入，则自动尝试加载同名侧车文件：
  - `xxx.cover.json`
  - `xxx_cover.json`
  - `cover.json`
- `core.py` 新增 `normalize_cover_info()`，允许空字段先输出封面
- `txt_to_docx_safe()` 默认也会生成空封面，避免直接缺页

**验证**：
- `output/A公司财务管理问题与对策研究_v3.docx` 已确认出现完整封面字段：
  - 学生：李雨奇
  - 准考证号：029818440007
  - 专业：财务管理
  - 层次：专升本
  - 指导教师：王艳
  - 题目：A公司财务管理问题与对策研究

---

### 1️⃣ `core.py` — 目录制表位单位修正

**问题**：WPS 中目录页码不右对齐、没有点引导。

**根因**：目录制表位 XML 的 `w:pos` 之前写成了 `EMU`，而 Word/WPS 识别的是 **twips**。

**修复**：
```python
tab_elem.set(qn('w:pos'), str(int(Cm(16).twips)))
```

**说明**：这属于代码实现问题，不是单纯的 WPS / LibreOffice 差异。

---

### 2️⃣ `renderer.py` — 去掉重复最终排版调用

**问题**：`txt_to_docx_safe()` 内部已完成 `finalize_docx()`，`renderer.py` 外层又调用一次，存在重复排版。

**修复**：
- 删除 `renderer.py` 中的二次 `finalize_docx()` 调用
- 让最终排版只发生一次，避免样式被重复修改

---

### 3️⃣ `core.py` — 目录页码回填强化

**问题**：部分目录项页码缺失。

**修复**：
- 增加二次兜底回填逻辑
- 对未匹配到 PDF 页码的目录项，沿用上一个已知页码补齐

**验证**：
- `output/test_final_v3.docx` 目录页码达到 `36/36`

---

### 1️⃣ `core.py` — 表格渲染修复

**文件位置**：`add_t()` 函数（第462行）

**问题**：表格数据列往左移，标题第一列字段丢失。

**根因**：原代码将 `data` 中的行数据拆出来放在前面，再用 `rows` 放行名，导致列对齐错乱。

**修复**：

```python
# 旧逻辑（错误）：
full_row = [row_name] + cells  # row_name重复出现

# 新逻辑（正确）：
# rows属性只放行名称 → 作为表格第一列
# data属性用 | 和 ; 分隔 → 作为数据单元格
# header属性 → 作为表头
```

**验证**：
- 表1 (4×4)：`指标 | 2021年 | 2022年 | 2023年` ✅
- 表2 (5×5)：`指标 | 第一季度 | 第二季度 | 第三季度 | 第四季度` ✅

**注意事项**：LLM 输出的 `<table/>` 标签的 `data` 属性必须严格遵循 `行1列1|行1列2;行2列1|行2列2` 格式。

---

### 2️⃣ `core.py` — 标签宽容解析修复

**文件位置**：`tolerant_extract_charts()` / `tolerant_extract_tables()` / `tolerant_extract_drawings()` 正则（第249、252、255行）

**问题**：LLM 输出的标签表格标签可能跨多行（属性值换行），原正则用 `(?=\n)` 作为结束符导致只匹配行首部分。

**修复**：

```diff
- pat = re.compile(r'<table\s+.*?(?:/>|>|(?=\n))', re.DOTALL)
+ pat = re.compile(r'<table\s+.*?(?:/>|>)', re.DOTALL)
```

`(?=\n)` → 删除 **`(?=\n)` 改为 `/>`**，支持跨行属性。

**验证**：204字符长的跨行 table 标签被正确解析。

---

### 3️⃣ `core.py` — 目录页码修复

**文件位置**：`finalize_docx()` 函数（第690-710行）

**问题**：最后5条 TOC 条目（5.3.2~参考文献）缺失页码。

**根因**：PDF 匹配失败（pdftotext 对末尾章节标题提取不全）+ `tab_stops` 回退代码在 `finalize_docx` 中因命名空间问题失效。

**修复**：

```python
# 增加 try/except 包裹每个条目
# XML 方式创建制表位（不依赖 python-docx API）
tabs_elem = __import__('docx').OxmlElement('w:tabs')
tab_elem = __import__('docx').OxmlElement('w:tab')
tab_elem.set(qn('w:val'), 'right')
tab_elem.set(qn('w:leader'), 'dot')
# ...
pPr.append(tabs_elem)
```

**验证**：standalone 测试 36/36 条全部有页码 ✅（集成调用中因模块缓存暂未生效，待确认）

---

### 4️⃣ `core.py` — 分节符/页脚处理

**文件位置**：`finalize_docx()` 函数（第745-810行）

**问题**：无页码、分节符位置不对。

**现状**：
- ✅ 1个分节符：TOC结尾处插入（`w:type=continuous` 连续分节符）
- ✅ 正文部分设置 `pgNumType start=1`（页码从第1章开始）
- ✅ 页脚插入 `PAGE` 域代码

---

### 5️⃣ `core.py` — 分页符处理

**文件位置**：`txt_to_docx_safe()` 函数（第920-1010行）

**问题**：关键词跑到了下一页；目录页没有"目录"二字。

**现状**：
- ✅ `---PAGE_BREAK---` 标记正确处理为 `pageBreakBefore`
- ✅ 7个分页符：目录、第2~6章、参考文献
- ✅ `目录` 居中显示（黑体18pt）
- ✅ 关键词保持在摘要页

---



### 4️⃣ `prompts/manage.py` + `core.py` — 图表协议升级与多系列兼容

**问题**：图表生成存在“维度缺失、系列被简化、图类型被压扁”为单线图的问题，尤其在“优化前后对比”“账龄结构”等图上更明显。

**修复**：
- `prompts/manage.py` 新增图表强约束：
  - 正文中的每个维度/系列必须在图表中完整出现
  - 多系列图必须显式输出 `legend`
  - 不得删年、删线、删列，不得用 `null` / 省略号逃避补齐
  - 增加 `series-major` 输出规范与对比图示例
- `core.py` 的图表渲染新增：
  - `legend` 解析
  - 多系列折线/柱状/堆叠/对比/甘特图支持
  - category-major 自动转置兼容

**验证**：
- `李雨奇` 样例中“资本结构优化前后对比”“应收账款账龄结构优化目标”已可正常渲染
- 多系列图不再被误判为单系列而跳过

---

### 5️⃣ `prompts/mechanical.py` + `core.py` + `generator.py` + `renderer.py` — 机械分支重构与图纸链路修复

**问题**：
- 机械论文正文存在场景漂移，前后混入车削 / 钻孔 / 铣削 / 卡盘 / 顶尖等不同工艺链
- `<drawing/>` 标签曾大量出现半成品、裸标签、重复 id，导致图片数量不足或错配
- 图纸图片目录存在旧图污染，图型和标题不一致
- 本地图示中文出现方块字，影响机械图纸可读性

**修复**：
- `prompts/mechanical.py`
  - 增加场景锁定规则：全文仅围绕同一机械对象、同一工艺链展开
  - 增加固定场景提示：夹具类统一锁定为“铣削工序专用夹具设计”
  - 机械提示词按专业分支细分：夹具 / 传动 / 送料 / 搬运 / 升降 / 结构优化
- `generator.py`
  - 新增机械图纸标签规范化：按出现顺序重编号，剔除裸 `<drawing/>` 和占位标签
  - 机械类输出在最终校验后再次清洗，避免脏标签落盘
- `renderer.py`
  - 每次自动生成图纸前清空旧图目录，避免旧图污染
  - 机械分支在渲染侧统一走图纸生成链路
- `core.py`
  - 图纸提取改为按出现顺序编号 `seq`，不再依赖原始 `id`
  - 图纸文件名按 `drawing_{seq}.png` 落盘，支持重复 id 的机械样例
  - `_load_font()` 加入中文字体，解决本地图示中文字方块问题
  - `generate_diagram_image()` 按图型细分为：
    - 受力分析图
    - 零件图
    - 传动图
    - 定位/装配/布局/结构图
    - 流程/时序图

**验证**：
- 机械样例连续回放后，`TXT` 与 `DOCX` 图纸数量一致
- 抽查结果显示图纸可读性明显改善，且未再出现旧图污染
- 夹具类样例已稳定锁定在同一工艺链，不再跨场景跳变

**当前结论**：
- 机械分支链路已从“能生成”推进到“可控生成”
- 仍建议后续在真实论文修改时继续抽查正文与图纸的逐章一致性

## 三、测试输出文件

| 文件 | 说明 | 状态 |
|------|------|------|
| `output/test_paper.txt` | 测试论文 TXT（含标签） | ✅ |
| `output/test_paper_fixed.docx` | 初始修复版 DOCX | ✅ |
| `output/test_pagefix.docx` | 页码修复测试 | ✅ |
| `output/test_v9.docx` | 全部36条TOC有页码 | ✅ 36/36 |

---

## 四、2026-07-05 设计类专项记录

### 1. 任务范围

- 对设计类论文进行整体重跑、抽检、纠偏
- 重点处理：
  - 摘要前缀 `摘要：`
  - 表格输出错误
  - 图纸标签粘正文
  - 图片空白/近白
  - 图纸分流错误
- 最终输出目录按批次递进保留：
  - `设计类_完整重生成_修正3`
  - `设计类_完整重生成_修正4`
  - `设计类_完整重生成_修正5`
  - `设计类_完整重生成_修正6`
  - `设计类_完整重生成_修正7`

### 2. 已确认的问题

#### 2.1 摘要问题

- 个别摘要开头残留 `摘要：` / `摘要:`
- 解决：在设计类摘要清洗中统一删除摘要前缀，只保留正文
- 结果：`paper.txt` 抽检中摘要前缀已清除

#### 2.2 表格问题

- 早期批次中出现 HTML 碎片表格、`<tr><td>` 残片、以及 markdown `|` 表格
- 还有第一列语义不清、出现数字占位的问题
- 解决：
  - 统一把设计类表格收敛为 `<table/>`
  - 设计类只保留两类模板：
    - 对比型：第一列使用 `对比项`
    - 评价/统计型：第一列使用 `评价维度` 或 `统计维度`
  - 表格 `rows` 必须是真实行名，不能是数字占位
- 结果：新批次未再出现 HTML 表格源码污染

#### 2.3 图纸标签问题

- 早期批次存在 `<drawing .../>` 后面继续粘正文、或者尾巴残留 `/`
- 还出现过同一图纸重复 id、半闭合标签、裸标签
- 解决：
  - 对 design markup 做了切分清洗
  - 删除孤立 `/` 行
  - 把 `drawing` 标签与正文尾巴拆开
  - 渲染前重新提取并按顺序编号
- 结果：`paper.txt` 里不再有明显的标签粘连残留

#### 2.4 图片空白问题

- 实际抽检发现大量“白底只剩几条线”的图片，尤其是平面图、立面图、节点图、分析图
- 这不是渲染丢图，而是图片生成分流和 prompt 约束不足
- 根因：
  - 图纸类型被错误分到过弱的本地图示后端
  - 提示词过于宽泛，导致模型输出空白或近白图
- 解决：
  - 设计类图纸统一走 GPT 生图
  - 平面/立面/节点/分析图使用专门的图示 prompt
  - 给 prompt 加入“必须占满画面、四角与中心都要有内容、至少 4 个标注/ callout、不能像草稿”的硬约束
  - 加入空白图检测，过白则自动重试
- 结果：
  - 总平面图、空间效果图已抽检正常
  - 节点/立面图的质量较旧版有明显改善

### 3. 设计类重跑过程

- `修正4`：先清理文本残留并重渲 `docx`
- `修正5`：开始按图片白度抽检，发现大量近白图
- `修正6`：尝试按新分流全量重跑
- `修正7`：进一步压实 GPT 图示 prompt，当前已抽到正常样例
- `修正8`：最终批次重跑完成，6 篇设计类论文均已重新生成并渲染完成，图片未再出现整页空白问题
- 复查发现部分篇目存在同内容重复 `drawing` 标签，渲染器会逐条插图，导致同一图纸内容重复生成；已在提取阶段增加按内容签名去重，后续只需针对受影响单篇重渲，不再全量重启
- 2026-07-05 补充清理：已对 `廖颖_029823410656`、`王刚_029823410665`、`蔡静瑶_029823410230` 去除重复图纸标签并重渲 `paper.docx`，三篇均已验证无重复 drawing 标签
- 2026-07-05 换题重写：`王轲_029823410126` 已按新题 `昆明西山区某闲置老厂房社区创客共享空间更新设计研究` 从画像到正文完整重生成；`张瀚文_029823410440` 按原题完整重写，二者已输出到 `设计类_完整重生成_修正9`

### 4. 当前可用结论

- 设计类图片问题的关键不在 DOCX 排版，而在“图纸分类 + prompt 强度 + 空白检测”
- 经过今天的修改，以下内容已稳定：
  - 摘要前缀清理
  - 表格标签规范化
  - 图纸标签粘连清理
  - 图纸按顺序提取与重编号
  - 设计类 GPT 生图分流

### 5. 明天接手时优先看什么

1. 直接从 `设计类_完整重生成_修正7` 接手
2. 优先抽查这几个文件：
   - `倪月华_029823410586/drawings_paper/drawing_1.png`
   - `廖颖_029823410656/drawings_paper/drawing_1.png`
   - `蔡静瑶_029823410230/drawings_paper/drawing_4.png`
3. 如果还出现空白图，继续只收紧图示 prompt，不再改正文生成逻辑
4. 如果 `docx` 与图片数量不一致，再查 `renderer.py` 的图纸加载逻辑

### 6.1 `修正8` 抽检结果

- `倪月华_029823410586`：`7` 张图，`paper.docx` 已生成
- `廖颖_029823410656`：`8` 张图，`paper.docx` 已生成
- `张芳_029822410430`：`5` 张图，`paper.docx` 已生成
- `王刚_029823410665`：`8` 张图，`paper.docx` 已生成
- `蔡静瑶_029823410230`：`7` 张图，`paper.docx` 已生成
- `马泽宇_029821410470`：`6` 张图，`paper.docx` 已生成
- 抽检的 `蔡静瑶`、`马泽宇` 图纸内容正常，未见空白页式图片

### 6. 备注

- 今天的设计类记录应视为后续所有设计类论文的默认修正基线
- 后续不建议再回到“只改一点点又扩大问题”的做法，应优先：
  - 先改分流
  - 再改 prompt
  - 最后才做局部文本修补
| `output/test_final.docx` | 集成测试输出 | ⚠️ 31/36 |
| `output/test_final_fixed.docx` | 直接修复版 | ✅ |
| `output/test_final_v2.docx` | 最终版 | ⚠️ 31/36 |

---

## 四、待解决问题（明日继续）

### 高优先级

1. **TOC 页码 36/36**
   - `finalize_docx` 内部的 `last_sec_page` 回退代码在 standalone 测试生效（test_v9）
   - 在集成调用 `txt_to_docx_safe → finalize_docx` 中5条缺失
   - **可能原因**：Python 模块导入缓存，需重启进程或 `importlib.reload()`
   - **修复方向**：`python3 -c "from importlib import reload; import core; reload(core)"` 或使用子进程调用

2. **表格标签格式可靠性**
   - `tolerant_extract_tables` 已修复多行问题
   - 但 LLM 输出的标签格式仍有微小变化风险（如单引号 vs 双引号、属性顺序变化）
   - **建议**：增加一层 LLM 校验（`validate_tag_format` 已存在，可加强）

### 中优先级

3. **分节符/页码最终检查**
   - 目前分节符处理逻辑在 `finalize_docx` 中
   - 需要验证 LibreOffice 渲染 PDF 后页码是否正确

4. **提示词优化**
   - `prompts/manage.py` 已增加图表格式约束、致命错误检查清单
   - 动态调整：可根据输出质量自动优化提示词

### 待规划

5. **新增专业**（机械、土木）
6. **Web 服务测试**（`web.py` 已基于 core/generator/renderer/reviser）

### 子任务执行约束

1. 子任务入口统一使用 `run_subtask.sh`
2. 默认走官方免费模型 `opencode/deepseek-v4-flash-free`
3. 若 `opencode` 调用失败，必须先停下排障，不允许切换为手工替代流程

---



## 四、阶段性总结（截至本轮）

### 1. 已完成的核心改造

- **生成/修改链路分离稳定化**
  - 生成走 `generator.py`，渲染走 `renderer.py`
  - 封面信息通过 JSON 侧车文件保留并自动加载
  - `renderer.py` 去掉重复最终排版调用，避免样式二次破坏

- **图表协议升级**
  - 经管类提示词从“只要求输出图表标签”升级为“维度完整性 + 多系列 + 数据补齐 + 图类型约束”
  - 新增 `series-major` 输出约束，减少图表被简化成单系列的问题
  - 设计类同步增加通用完整性约束，但保留主设计图/效果图原流程不变

- **图表渲染能力增强**
  - `core.py` 支持 `legend`
  - 支持多系列折线、柱状、堆叠、对比、趋势、甘特图、结构图
  - 对 category-major 输入做了兼容转置，降低历史输出格式冲突
  - 图型不支持时明确报错并跳过，不再画空白图

- **目录/封面/页码修复**
  - 封面支持空字段先输出，后续可补录
  - TOC 页码回填做了二次兜底
  - 目录点引导和页码右对齐问题已按 Word/WPS 兼容方向修正

### 2. 已验证结果

- 重新生成的 10 位学生论文中，9 位已完成重生成并补齐 `paper.txt` / `paper.docx`
- 李雨奇样例中曾出现的多系列图错配问题，已通过提示词与渲染兼容一起压住
- 设计类提示词已同步“完整性”逻辑，但主设计图/效果图暂不改动

### 3. 仍需关注的事项

### 4. 机械设计分支已启动

- 新增 `prompts/mechanical.py`，按机械设计方向模板化生成
- 章节结构固定为：概述 → 原理与方案分析 → 总体设计 → 关键部件设计 → 计算与校核 → 总结
- `profile.py` 已支持机械画像生成
- `generator.py` 已支持机械论文生成与机械图纸分析提示词
- `reviser.py` / `web.py` 已补充机械类型入口
- 机械类仍保持普通图片级图纸方案，后续可再细化图像后端

- 个别图表仍可能出现：
  - series-major / category-major 混用
  - 甘特图任务数与分类数不一致
  - LLM 格式校验 JSON 解析失败（但已不阻断主流程）
- 后续优先继续优化提示词稳定性，而不是增加程序语义解析



## 六、程序功能结构确认

### 1. 直接生成
- 用户或 AI 辅助先生成题目与画像
- 再进入论文生成流程
- 封面处理：
  - 信息完整则生成完整封面
  - 信息不全则先输出空字段封面，但保留论文题目

### 2. 修改生成
- 用户提供待修改论文
- 程序先审核、诊断
- 以“重新生成”为核心逻辑完成修改
- 最终结果可能与原文差异较大，但满足生成要求

### 3. 按专业分流
- 程序按专业分类采用不同生成逻辑
- 差异主要体现在：提示词、章节结构、图表/图像规则、画像逻辑

### 4. Web 服务化
- 程序可封装为 Web 服务
- 前端可按用户要求调整交互与流程
- 后端根据用户输入决定生成/修改、专业类型、封面策略等

## 五、测试方法

```bash
cd /root/project/workspace/thesis-reviser

# 1. 单独测试页码修复
python3 -c "from core import finalize_docx; finalize_docx('output/test_xxx.docx')"

# 2. 完整集成测试
python3 -c "
from core import txt_to_docx_safe
txt_to_docx_safe('output/test_paper.txt', 'output/test_result.docx')
"

# 3. 验证结果
python3 -c "
from docx import Document
doc = Document('output/test_xxx.docx')
# 检查表格、页码、分页符等
"
```

---

## 五、本轮最终修改（2026-07-01）

### 1. `core.py` — 机械工程图改用 GPT 生成

**问题**：SVG 本地绘制的工程图过于模板化、抽象，不同图之间高度雷同，且一眼看不出是夹具/模具等专业对象，不满足毕业论文对工程图专业度的要求。

**修复**：
- 新增 `_build_engineering_image_prompt(drawing)`：为每张机械工程图构造受控的 GPT 图像生成 prompt
- 在 prompt 中强制要求：
  - 正交二维前视图，禁止透视和 3D 渲染
  - 必须包含夹具体、工件、定位销、V 形块、压板、螺栓、铣刀（或模具的上/下模座、凸模、凹模、导柱等）
  - 剖面线、中心线、尺寸线、件号气球、标题栏
  - 所有尺寸必须匹配 description 和 machine_spec 中的参数
  - 禁止发明未提及零件、禁止修改参数、禁止阴影/渐变色
- 修改 `generate_single_image()`：机械类 diagram 优先走 GPT 工程图，失败后再回退到 SVG 本地模板
- 新增 `_call_image_api()` 统一封装图像 API 调用，减少代码重复
- 图像 API 超时时间从 120 秒提升到 300 秒，适配 GPT 图像生成耗时

**影响**：
- 机械论文的 14-17 张工程图全部可由 GPT 生成
- 单图耗时约 20-40 秒，14 张图总计约 10 分钟
- 图纸风格明显专业化，且结构图、参数图、受力分析图、装配图之间不再重复

---

### 2. `svg_engineering_drawing.py` — SVG 兜底模板升级

**问题**：原 SVG 模板是通用方块/箭头/圆圈，无法识别为具体机械对象。

**修复**：
- 新增 `fixture`（夹具）专用模板：底座、工件、V 形块、定位销、压板、螺栓、铣刀、尺寸标注、明细栏、标题栏
- 新增 `mold`（模具）专用模板：上模座、凸模固定板、凸模、卸料板、板料、凹模、凹模固定板、下模座、导柱
- 改进 `force`（受力分析）、`assembly`（装配）、`flow`（流程）、`motion`（运动）、`parameter`（参数）、`structure`（结构）模板
- 增加剖面线填充、中心线、尺寸箭头、件号气球、标题栏

**定位**：SVG 作为 GPT 失败时的兜底方案，保留在 `mechanical_cad.py` 中调用。

---

### 3. `prompts/mechanical.py` — 表格格式与数据一致性强化

**问题**：
1. 机械类 `<table/>` 标签的 `data` 属性使用逗号分隔，导致表格数据全部挤入第二列
2. 机械类论文章节之间参数不一致（如定位销直径、夹具体尺寸前后变化）

**修复**：
- 表格格式示例改为 `|` 分隔单元格、`;` 分隔行，明确 `data` 列数 = `header` 列数 - 1
- 给出对比表和参数表两种完整示例
- 新增 `CONSISTENCY_RULE`（数据一致性铁律）：
  - 全文同一参数必须一致
  - 必须使用 machine_spec 中的载荷、精度、行程、材料、工况等值
  - 首次出现的参数后续章节必须沿用
  - 每张图纸 description 必须包含至少 2 项 machine_spec 关键参数

---

### 4. `generator.py` + `core.py` — machine_spec 注入与跨章一致性

**问题**：图纸渲染时无法获取全局 machine_spec，导致图纸标注与正文、machine_spec 不一致。

**修复**：
- `generator.py`：将 `machine_spec` 写入每个章节的 `[[MECH_JSON]]` 块
- `core.py`：新增 `_extract_machine_spec_from_blocks()`，从 MECH_JSON 块中提取全局 machine_spec
- `core.py merge_mech_json_into_drawings()`：将 machine_spec 的载荷、精度、行程、材料、工况、关键参数注入到每张图纸的 `numeric_annotations`
- `svg_engineering_drawing.py` 优先使用 `numeric_annotations` 中的规范参数生成标注

**效果**：图纸上的关键参数与 machine_spec、正文保持同一来源。

---

### 5. `generator.py` — 计算章节自洽检查

**问题**：计算与校核章节中，LLM 可能产生明显自洽错误的数值（如夹紧力比设计指标大 10 倍以上）。

**修复**：
- 新增 `_check_calculation_consistency()`
- 检查计算得到的最大夹紧力/载荷是否超过 machine_spec 设计指标 10 倍
- 若异常，调用 LLM 重写计算章节，使数值链自洽

---

### 6. `README.md` — 程序整体使用说明

**新增文件**：`thesis-reviser/README.md`

**内容覆盖**：
- 程序简介与支持的三种论文类型（管理/设计/机械）
- 目录结构与核心模块说明
- 环境依赖与字体要求
- 快速开始示例（管理/设计/机械）
- 各模块 CLI 详细用法
- `<chart/>`、`<table/>`、`<drawing/>` 标签格式示例
- 封面信息 JSON 格式
- 常见工作流与注意事项

---

## 六、本轮测试输出

| 文件 | 说明 |
|------|------|
| `output/mech_test_paper_v7.txt` | 机械类论文文本（含标签） |
| `output/mech_test_paper_v7.docx` | 最终排版后的机械论文 DOCX |
| `output/drawings_mech_test_paper_v7/` | 14 张 GPT 生成的工程图 PNG |
| `output/mech_test_profile.json` | 机械类测试画像 |
| `output/mech_test_paper_v5.docx` / `v6.docx` | 中间版本（SVG 与 GPT 过渡） |

**测试结果**：
- v7 全部 14 张工程图由 GPT 成功生成，无 SVG 回退
- 表格列分布正确，无挤列现象
- 各图风格不同（结构图、参数图、受力图、装配图等），具备工程图基本要素
- 计算章节数值链基本自洽

---

## 七、最终方案说明

本轮最终确定的技术方案：

1. **工程图生成**：机械类工程图优先使用 GPT 图像 API，通过严格 prompt 控制风格与参数一致性；SVG 作为失败兜底。
2. **效果图生成**：设计类效果图仍使用 GPT 图像 API（未改动）。
3. **数据一致性**：通过 `machine_spec` 锁定同一参数源，注入图纸标注，并强化 prompt 约束。
4. **表格格式**：`<table/>` 使用 `|` 分隔单元格、`;` 分隔行，与 `core.py` 渲染器严格对应。

---

## 八、本轮经管类问题修复

### 1. `prompts/manage.py` — 修复格式禁令冲突并强化表格约束

**问题**：
- 原提示词把 `>` 和 `|` 一起列入了“markdown禁令”，但 `<chart/>` 需要 `/>`，`<table/>` 需要 `|` 和 `;`，存在明显冲突。
- 虽然写了“必须有表格”，但约束位置偏后，模型仍容易只输出图表不输出表格。

**修复**：
- 调整 `FMT_RULE`，不再把标签必需符号与 markdown 禁令混在一起。
- 明确说明正文中禁止 markdown，但标签内部允许必要属性符号。
- 将表格要求提前并加重：
  - `现状分析`、`问题与原因分析`、`解决方案` 每章至少 1 个 `<table/>`
  - 有图表分析时优先补同源表格
  - 表格数据必须用 `|` 分列、`;` 分行

### 2. `generator.py` — 经管类标签规范化与表格兜底

**问题**：
- 管理论文文本中经常出现 `<chart .../>/`、`<chart ..."/` 这类坏标签。
- 图表 `id` 会重复，导致后续解析时覆盖或缺图。
- 没有表格时，渲染链路只能得到图，无法满足结构化数据要求。

**修复**：
- 新增 `normalize_manage_markup()`：
  - 自动修复 chart/table 的闭合符
  - 统一重排全局唯一 `id`
  - 如果某段只有图没有表，则自动把首个图表转成同源明细表
- 管理类生成流程改为调用该后处理，不再误用设计类的标记清理逻辑。

### 3. 本轮测试结果

**输入**：
- `output/mgmt_test_paper.txt`

**输出**：
- `output/mgmt_test_paper.txt`
- `output/mgmt_test_paper.docx`

**检查结果**：
- 图表：9 个
- 表格：8 个
- 未闭合标签：0
- 重复 `id`：0
- DOCX 渲染成功，图表与表格均能正常进入排版结果

**补充验证**：
- 使用 opencode 子任务复查了 `output/mgmt_test_paper.txt`
- 结果与本地检查一致，确认标签数量、唯一性与闭合状态均正常

---

## 九、批处理接入与重复内容去重

### 1. `generator.py` — 经管类重复内容去重与表格修复

**问题**：
- 批量生成的经管类论文里，同一组图表数据会在不同章节重复出现。
- 部分表格把整行数据挤进了 `rows`，并且缺少 `data`，导致渲染时表格列错位或表格标记无法规范化。

**修复**：
- 新增按数据结构生成的图表/表格签名，去掉重复内容，只保留第一份。
- 新增表格修复逻辑：
  - 当 `data` 缺失但 `rows` 里塞了整行数据时，自动恢复为标准 `rows + data`
  - 对缺少闭合符的标签统一修复为 `/>`
- 兜底表格策略改为：
  - 若全文已有有效表格，不再为每个图表重复补表
  - 仅在全文完全没有表格时，才补 1 张兜底表

### 2. `batch_generate.py` — 新增批处理入口

**新增文件**：`batch_generate.py`

**功能**：
- 读取 `output/batch_titles.json`
- 按学生逐个生成 `profile.json`、`cover.json`、`paper.txt`、`paper.docx`
- 复用 `generator.py` 的去重和表格修复逻辑
- 适合批量重跑 `output/batch_run/` 目录

### 3. 本轮批处理验证

**测试目录**：
- `output/batch_run_test3/李雨奇_029818440007/`

**结果**：
- `<chart/>`：7 个
- `<table/>`：6 个
- 未闭合标签：0
- 重复 `id`：0
- DOCX 成功渲染
- opencode 子任务复查结果一致

---

## 十、设计类问题总闭环（截至 2026-07-05）

> 这一段用于把最近两天的设计类问题统一收口，避免后续只记“修了什么”，不记“为什么修、修到什么程度”。

### 1. 问题总览

- 摘要段落中残留 `摘要：` / `摘要:`
- 表格生成失败，出现 HTML 残片、Markdown 表格、标签错位、列数错乱
- 表格第一列语义不清，出现数字占位或把多列内容挤进第二列
- `<drawing/>` 标签与正文粘连、重复 id、重复标签、裸标签
- 图片空白、近白、重复生成，导致一张图被多次消耗 token
- 部分设计类题目方向不准，需要换题重写，且必须先给题目确认
- 个别篇目需要只做定点修改，不能扩大到整篇重排

### 2. 已完成的闭环修复

- 摘要问题：
  - 在设计类清洗链路中删除摘要前缀，只保留正文
  - 解决后，摘要段落不再出现多余 `摘要：`

- 表格问题：
  - 将设计类表格统一归一为 `<table/>`
  - 约束第一列必须是语义字段，如 `对比项` / `评价维度`
  - 补强表格解析与重建逻辑，避免 HTML 碎片、Markdown 表格、`td` 残片落盘
  - 针对“后两列跑到第二列”的问题，增加表格 payload 重新拆分与列对齐逻辑

- 图纸问题：
  - 清理 `<drawing/>` 粘正文、半闭合标签、重复 id、重复标签
  - 图纸提取按顺序重编号，避免旧图污染
  - 图纸生成统一走更强约束的 GPT 生图链路
  - 增加空白图检测与重试，减少白图和近白图

- 题目与重写问题：
  - 对明确要求重写的篇目，改为“画像到正文完整重生成”
  - 对需要先确认题目的篇目，先给题目过目，再调用生成程序
  - 已完成的重写篇目集中输出到新的重生成目录，旧目录保留，不回写污染

- 重复图问题：
  - 已确认重复图来自多次生成、重复标签或同内容多次进入渲染链路
  - 通过去重与单篇重渲处理，避免重复占用图片生成 token

### 3. 典型案例收口

- 王轲 / 张瀚文：
  - 王轲已按新题从画像到正文完整重写
  - 张瀚文按原题完整重写
  - 两篇都已在新批次完成表格修复，列错位问题已消除

- 设计类批次：
  - 摘要前缀、表格碎片、图纸粘连、空白图、重复图等问题都已逐类处理
  - 当前批次输出结果已形成新的稳定基线

### 4. 经验结论

- 设计类问题不能只靠后处理修补，必须同时约束：
  - prompt
  - 标签规范化
  - 渲染前清洗
  - 渲染后抽检
- 对于明确的定点问题，优先做局部修复，不要动不动全量重启
- 对于重写类需求，必须先确认题目，再从画像开始整链路重跑
- 后续新增设计类论文时，应默认继承本次闭环规则，不再回到旧的松散输出方式

---

## 十一、本轮新增生成（张嘉瑜 / 顾安奇）

### 1. 张嘉瑜（029823410013）

- 专业：环境设计
- 拟题并生成：`南宁市青秀区凤岭北街道某老旧社区共享庭院更新设计研究`
- 生成链路：画像 → 正文 → 图纸 → DOCX
- 输出目录：`设计类_完整重生成_张嘉瑜_029823410013`
- 结果：
  - `paper.txt` 已生成
  - `paper.docx` 已生成
  - 图纸 6 张，已落盘到 `drawings_paper/`
  - 抽检通过，未见摘要前缀残留，未见空白图

### 2. 顾安奇（029820430393）

- 专业：旅游管理
- 拟题并生成：`西安大唐不夜城游客体验优化研究`
- 生成链路：画像 → 正文 → DOCX
- 输出目录：`经管类_完整重生成_顾安奇_029820430393`
- 结果：
  - `paper.txt` 已生成
  - `paper.docx` 已生成
  - 表格与目录排版正常，未见摘要前缀残留

### 3. 本轮确认点

- 题目已先行明确，再执行全文生成
- 设计类继续沿用“具体对象 + 图纸生成 + 空白图抽检”流程
- 经管类继续沿用“具体案例 + 表格约束 + 目录页码修复”流程
- 两篇均已纳入新的输出目录，便于后续单篇抽检与局部修改

### 4. 2026-07-05 经管类表格收紧补充

- 新发现问题：部分经管类表格把“行名 + 数值 + 结论”整行塞进 `rows`，导致渲染时第一列和后续列整体错位
- 根因：LLM 输出的 table schema 不稳定，后处理只做了闭合和去重，没有把污染后的 `rows` 重新拆回矩阵
- 新修复：
  - `core.py` 的表格矩阵规整逻辑加入“整行展开”识别：当 `rows` 明显是按整行拍平的内容时，直接按列数切回二维矩阵
  - `prompts/manage.py` 强化表格硬约束：`rows` 只能放纯行名，数字/年份/百分比/结论一律进入 `data`
  - 已对 `顾安奇_029820430393` 做无 LLM 回炉清洗并重新渲染，当前 DOCX 中的表格已恢复为标准结构

---

## 十一、土木工程修复总闭环（2026-07-05）

### 1. 修改文件

| 文件 | 改动 |
|------|------|
| `core.py` L2686-2694 | 新增 `is_dotted_repeat` 检测：`X. 同标题`（如 `2. 建筑设计`）→ 跳过 |
| `core.py` L2723-2725 | 文本段落渲染前添加跳过逻辑 |
| `prompts/civil.py` | 重写 DRAWING_RULE：description 规则改为"全文本描述性生图提示词" |
| `batch_generate_civil.py` | 第1章（工程概况）也追加 DRAWING_RULE，保证表1-1用 `<table>` 渲染 |

### 2. 修复项

#### 2.1 表1-1绑定到工程概况章节 ✅
- **问题**：第1章"工程概况"原本不走 DRAWING_RULE，LLM 输出的表1-1是纯文本（`表1-1 主要设计参数汇总`），不是 `<table>` 标签
- **修复**：`batch_generate_civil.py` 中 `chapter_prompts[0]` 追加 DRAWING_RULE 约束
- **结果**：表1-1现在以 `<table id="1" title="表1-1 ..." headers="参数名称||参数值" data="..."/>` 格式输出

#### 2.2 表3-1/表4-1 "层号"空列修复 ✅
- **问题**：`add_t()` 中 `rows=""` 时数据行仍被加了一个空白的 `row_name` 列，导致表头5列 vs 数据行6列
- **修复**：`core.py` 中 `add_t()` 跳过 `row_name` 前置，当 `rows=""` 时数据行列数 = `len(headers)`
- **结果**：表3-1(5列)、表4-1(6列) 渲染正常，无空列

#### 2.3 裸drawing过滤修复 ✅
- **问题**：`_canonicalize_drawing_line()` 对无 `id` 属性的 drawing 自动赋 `seq` 值，导致过滤条件 `not parsed.get("id")` 永远不触发
- **修复**：`extract_drawings_from_text()` 中改为检测原始行文本是否包含 `id=` 或 `title=` 属性
- **结果**：无 `id=` 的裸 drawing 被正确过滤

#### 2.4 drawing description 全文本化 ✅
- **问题**：LLM 输出的 drawing description 是简短摘要（如"标准层柱网布置图"），GPT生图时信息不足
- **修复**：DRAWING_RULE 中重写 description 规则——必须包含所有相关正文文本，写成可直接用于 GPT 生图的 prompt
- **结果**：如 `图4-1 ⑦轴框架计算简图` 的 description 包含完整的结构信息

#### 2.5 带点章节标题重复行清洗 ✅
- **问题**：LLM 输出第2章标题为 `第2章 建筑设计` 后，紧跟一行 `2. 建筑设计`（带点强调格式），作为普通文本被渲染到正文
- **修复**：`core.py` `txt_to_docx_safe()` 中增加 `is_dotted_repeat` 检测：在 `pt_last_heading` 不为空时，检测 `X. 文字` 格式与上一级标题内容是否重复
- **结果**：严涛测试 DOCX 中 `2. 建筑设计` 被成功跳过，第2章后直接为 `2.1 建筑平面设计`

### 3. 测试结果（严涛）

| 项 | 结果 |
|----|------|
| 图片 | 13张，全部 >1MB（无空白图） |
| 裸drawing | 0个 |
| 表1-1 | `<table>` 标签，2列17行，渲染正常 |
| 表3-1 | 5列无空列 |
| 表4-1 | 6列无空列 |
| 章节重复 | `2. 建筑设计` 被跳过 |
| drawing description | 完整文本描述 |

### 4. 下一步
- 批量生成剩余22篇土木论文
- 30分钟检查一次进度

---

## 十二、最终汇总（当前收口版）

### 已完成且已验证

- 生成链路与渲染链路分离稳定：`profile → generator → renderer → DOCX`
- 封面侧车文件自动加载、空字段封面兜底
- 目录页码回填、点引导、页脚、分节符处理
- 目录强制单独起页
- 经管类表格硬约束与矩阵重建
- 设计类摘要前缀清理、表格/图纸污染清理、重复图去重、空白图检测
- 机械类图纸 GPT 生图与 machine_spec 一致性
- 批处理入口 `batch_generate.py`
- 张嘉瑜、顾安奇两篇新生成已落盘

### 进行中

- 土木工程批量生成与图片质量控制仍在另一条记录中持续推进
- 个别论文仍可能需要单篇抽检后做局部修补，但不应再全量重启

### 待办

- 新会话中对程序做最后修订
- 若继续批量生成，优先沿用本轮已收口的全局规则，不再回退到旧版松散输出

### 记录归类说明

- `[今日进度_20260704.md](/root/project/workspace/thesis-reviser/今日进度_20260704.md)` 属于另一条任务线的阶段记录，内容偏土木，不应混入当前主线结论
- `[修改记录.md](/root/project/workspace/thesis-reviser/修改记录.md)` 为主记录，但中间存在多位 agent 追加内容，后续以“已验证生效/仅记录未复核”两个层级读取


## 2026-09-06/07 论文终版 273 篇 docx-only 审计与修复（agent 执行）

- **摘要区**：全量扫出 8 篇（7 篇正文带"摘要："前缀、3 篇摘要标题下夹论文标题行），python-docx 修复+复扫 0 残留；备份 `论文终版/_修改备份/摘要修复/`
- **图片**：审计 29 篇 ❌ → 9 篇系目录行"标题+页码"粘连误报（构词防升级后消除），真问题 20 篇 docx 级手术：AI 生图 27 张（≈$0.3）补图/造注、10 条图注补编号、图8/9 补标题；终验 273 ✅ 0 ❌
- **加固轮**：新七维（表注双重/孤儿/跳号、LaTeX 残留、占位符、图宽、图注跳号、参考文献）抓出 8 篇 10 处：删孤儿表注 7、表注改号+引用同步 3、张超 LaTeX 公式清洗 6；全绿
- **工具沉淀**：`audit_docx_only2.py`（终审审计）、`fix_final_images.py`（四类修复）；命名空间 `ns0:` 归一、TOC 粘连防、孤儿表注判定（注后无表体）为本轮新方法论
- **输出**：`/root/project/workspace/论文终版_修订0907/`（28 篇修改版+修改清单.csv）；原件就地更新，备份 `_修改备份/{图片修复,摘要修复,表注修复}/`

### 2026-09-07 补：张超第5章裸行图注收口

- 裸图注维度上线后全量扫：273 篇仅张超 1 篇命中（txt 源 LLM 把三个 drawing 标签写残成"5-1/5-2/5-3 标题"裸文本行，图从未生成）
- 修复：三行补"图"字头；图5-1/5-2 AI 生图（AIHubMix 余额耗尽前完成）；**图5-3 用用户网页版供图**（飞书附件 sha256 取回，与论文 CAD 线稿风格一致）插入 13.5cm 居中
- 第5章终态图注3=内嵌3，273 篇终验全绿；修改清单已补记

### 2026-09-07 补：严涛 graphic 标签转义残留修复（用户发现"缺图缺表"）

- 根因：txt 源用 `<graphic>` 标签（非标准 `<drawing>`），旧渲染器不识别 → 标签以 `&lt;graphic...` 转义文本裸躺正文，图注图全丢
- 修复：2 处转义行 → 规范图注"图2-1 平面线形与GPS控制点布置示意图""图2-2 纵断面设计及竖曲线要素图"（第2章编号）；2.1/2.3 段尾补引用句；AI 生图 3 张（图2-1/2-2 新插 + 图1-1 错题图替换，图注同步贴题改"GPS控制网布置示意图"——首轮 B 类兜底曾误取目录行"工程概况 1"作标题）
- audit 新增维度：`&lt;graphic`/`&lt;drawing`/`&lt;table` 转义残留（273 全量 0 命中）
- 严涛终态：图注4=内嵌4，引用 1-1/2-1/2-2/5-1 全命中；273 篇全绿

### 2026-09-07 补：同类问题全量清查（表格标签裸躺两形态+孤儿表注）

- **管伟琪**(26土木)：`<table id="tab2-1" caption="各土层…">` 标签裸躺正文（严涛 graphic 的表格版，26届无txt源）→ 规范表注"表2-1 各土层主要物理力学性质指标汇总表"+表体5行，数值严格取自正文第2章土层描述（fak 仅第5层280kPa系正文明确值，其余列留—不编造）
- **张德轩**：表4-1"路面结构设计参数"孤儿表注（txt 源即为孤儿）→ 补表体11行（参数全部取自正文4.2：BZZ-100/0.7MPa/10.65cm/5+7+20+18cm等）
- **黄浩**：摘要段尾粘冗余 HTML 表格残留（三大核心问题，摘要正文已叙述）→ 删除
- audit 修正两误报源：表注正则排除"表N的…"叙述行；转义残留只认标签字母类（&lt;50% 系合法小于号转义非残留）
- 复扫 273 篇 ✅ 全绿；txt源对账 50 篇图意图=内嵌图数全匹配（无第二个严涛）

### 2026-09-07 补：严涛4.4裸标题+括号参数行（用户发现）

- 形态：`路面结构层示意图` 裸标题行 + `(自上而下：4cmAC-13C+…总厚度74cm，图中标注各层厚度及路拱横坡2%)` 括号参数行——"图中标注"字样表明本应有图，txt 阶段未生成
- 修复：AI 生图（五层结构+每层厚度标注+路拱2%+总厚74cm）→ 图注"图4-1 路面结构层示意图(分层参数)"（括号行并入图注），4.4 首段尾补"如图4-1所示"
- audit 新增维度：`裸标题行(…示意图/布置图/剖面图)+下行^(…图中` 模式（273 全量 0 命中，严涛唯一）
- 273 篇复扫全绿

### 2026-09-07 补：合并实验分支+数据一致性闸门调优（用户拍板：闸门必须、Plus网页生图服务器不做）

- **合并** experimental/mechanical-local-drawings：土木/机械一致性闸门(civil/mechanical_consistency.py)+本地工程配图(civil/mechanical_figures.py)+图片缓存熔断+可选fallback(Plus网页桥仅环境变量，服务器不配置即跳过)；修 GBK 乱码 bug("鏈烘"→"机械"×2)
- **闸门接入双层**：renderer.render()(渲染前)+audit_docx_only2(终验)，土木论文参数冲突→❌
- **闸门调优**(宁窄勿宽，零误报优先)：
  - 层高/板厚拆 key(首层/标准层、楼板/屋面板)——各层各部位语义不同
  - 值域过滤(层高2.4-6.5m/总高3-120m)——表1-1单元格拼接伪影("标准层层高16.35m"实为总高串格)
  - 总工期只认真总工期，前18字含分部限定词(结构/阶段/每层/装修…)丢弃
  - 分部位参数(砼等级/梁截面/板厚)降 warn——垫层C15/主体C30 属正常工程实践
  - 数值归一(3.60==3.6)；audit 加 KNOWN_RESIDUAL 已登记遗留机制(登记≠掩盖:CSV注明)
- **实修2篇**：冯然标准层高4.2→3.6m(漂移)；周亮标准层高2.9→3.0m+结构高度19.8→33.6m(11层物理不可能)，风振句19.8m 因支撑各层wk计算表登记暂留
- 273 篇终扫全绿

### 2026-09-07 补2：按用户拍板收窄合并范围（只有闸门）

- **回退**：本地工程配图(civil/mechanical_figures.py)与图片缓存熔断/Plus网页桥fallback(core.py/.env.example/README)——生图继续走 GPT(AIHubMix gpt-image-2)
- **Plus网页桥结论**：实验分支只有 IMAGE_FALLBACK_API_URL 环境变量占位，桥本体(浏览器登录态)从未在仓库，服务器做不了，连占位一并退掉
- **保留**：数据一致性闸门(civil/mechanical_consistency.py)双层接入(renderer THESIS_SOURCE_TEXT 已随 core 回退移除，闸门在 audit_docx_only2 终验层运行)+闸门调优+冯然/周亮实修
- 回归：273 docx 全绿、50 txt 审计 hard 4(=已修两案)不变

### 2026-09-07 补3：漂移治理+空承诺闭环（两轮讨论落地）

按用户拍板：无源审计不做（今后保留txt），落地生成端+检测端改造：
- 事实卡注入(土木)/章节检查点(土木+机械,冲突带反馈重写本章)——漂移在产生处被杀
- T9空承诺指代解算(无编号"如图/图中"→同节锚点对账,零误报)+T10自报清单对账
- rescue_refs补偿器(R1裸标题+括号→标签,严涛4.4形态可全自动)
- prompt输出纪律+自报约定;抽取器副词容差
- 273全绿,50txt hard=4(=已修两案)不变
